@*<div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
    <ol class="carousel-indicators">
        @{
            var counter = 0;
        }
        @foreach (var item in ViewBag.Carousel)
        {
            if (counter == 0)
            {
                <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
            }
            else
            {
                <li data-target="#carouselExampleIndicators" data-slide-to="@counter"></li>
            }
            counter++;
        }
    </ol>
    <div class="carousel-inner">
        @counter = 0
        @foreach (var item in ViewBag.Carousel)
        {
            if (counter == 0)
            {
                <div class="carousel-item active">
                    <img class="d-block w-100 img-responsive" src="@item.Image" alt="First slide">
                </div>
                counter++;
            }
            else
            {
                <div class="carousel-item">
                    <img class="d-block w-100 img-responsive" src="@item.Image" alt="">
                </div>
            }
        }

    </div>
    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="sr-only">Next</span>
    </a>
</div>*@

<div class="fadee">
@foreach (var item in ViewBag.Carousel) { 
    <div>
        <img src="@item.Image" style="width: 100vw; height:400px;" />
    </div>
}
</div>