@model IEnumerable<School.Models.CourseLevel1>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LevelTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LevelTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CourseLevelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CourseLevelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CourseLevelId })
        </td>
    </tr>
}

</table>
